apiVersion: ansibleproviderconfig.k8s.io/v1alpha1
kind: AnsibleClusterProviderConfig
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: ansibleclusterproviderconfig-sample
spec:
  # Add fields here
  foo: bar
